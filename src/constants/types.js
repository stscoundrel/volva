const ARRAY = 'array'
const BIGINT = 'bigint'
const BOOLEAN = 'boolean'
const FLOAT = 'float'
const FUNCTION = 'function'
const INTEGER = 'integer'
const MAP = 'Map'
const NAN = 'NaN'
const NULL = 'null'
const NUMBER = 'number'
const OBJECT = 'object'
const SET = 'Set'
const STRING = 'string'

module.exports = {
  ARRAY,
  BIGINT,
  BOOLEAN,
  FLOAT,
  FUNCTION,
  INTEGER,
  MAP,
  NULL,
  NAN,
  NUMBER,
  OBJECT,
  SET,
  STRING,
}
