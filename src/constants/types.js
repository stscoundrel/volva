const ARRAY = 'array'
const BOOLEAN = 'boolean'
const FLOAT = 'float'
const FUNCTION = 'function'
const INTEGER = 'integer'
const MAP = 'Map'
const NAN = 'NaN'
const NULL = 'null'
const NUMBER = 'number'
const OBJECT = 'object'
const STRING = 'string'

module.exports = {
  ARRAY,
  BOOLEAN,
  FLOAT,
  FUNCTION,
  INTEGER,
  MAP,
  NULL,
  NAN,
  NUMBER,
  OBJECT,
  STRING,
}
