const ARRAY = 'array'
const BOOLEAN = 'boolean'
const FLOAT = 'float'
const INTEGER = 'integer'
const NULL = 'null'
const NUMBER = 'number'
const OBJECT = 'object'
const STRING = 'string'

module.exports = {
  ARRAY,
  BOOLEAN,
  FLOAT,
  INTEGER,
  NULL,
  NUMBER,
  OBJECT,
  STRING,
}
